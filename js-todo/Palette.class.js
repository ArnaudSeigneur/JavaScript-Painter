// Classe Palette
class Palette {

	constructor(selector) {
		// Propriété contenant la balise canvas du degrade
		// this.canvas

		// propriété contenant le context
		// this.ctx

        // Sizing du canvas
		// this.width
		// this.height

        // Appel de la methode de construction du (double) degrade
		// this.build();
	}

    // Methode de construction du degrade
	build() {
        // DOC : createLinearGradient()
        // DOC : addColorStop()

		// Gradient 1
		// Dégradé rouge -> vert -> bleu horizontal.

		// Gradient 2
		// Dégradé blanc opaque -> transparent -> noir opaque vertical.
	}

    // Methode de recuperation de couleur dans le degrade
	getColor(x, y) {
        // DOC : getImageData()
	}

    // CHALLENCE BONUS : le color zoomer
	zoomColor(x, y) {
        
	}

}
