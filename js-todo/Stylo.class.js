// Classe Stylo
class Stylo {

	constructor() {
        // Configuration general du stylo
		// this.posX
		// this.posY
		// this.color
		// this.thickness
	}

    // Methode pour definir (set) la couleur du stylo
	setColor(color) {
		this.color = color;
	}

    // Methode pour definir (set) l'epaisseur du stylo
	setThickness(px) {
		this.thickness = px;
	}

    // Methode pour stop le flux du dessin
	stopDrawing(ardoise) {

	}

    // Methode pour demmarer le flux du dessin
	startDrawing(e, ardoise) {

	}

    // Methode de dessin
	draw(ardoise, toX, toY) {
        // si mon adroise a le droit de dessiner
            // dessiner
	}

}
